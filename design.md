# RawBlock Design System Specification

> 本ドキュメントは **実装コード（`index.css` 等のスタイル定義）を正** とします。
> トークンの値やカラーを変更する場合は **まず CSS ファイル / 設定ファイルを編集** し、本ドキュメントを追従させてください。

## Overview

RawBlock は「現場や多様な環境において、視覚的に迷わず素早く・確実に操作できること」を最優先した、ブルータリズム志向のフラットデザインシステムです。不要な装飾を完全に排し、**太い枠線・高コントラスト・等幅フォント**で情報の境界と操作対象を明快に表現します。角丸（border-radius）やドロップシャドウを排除し、奥行きや状態変化は「枠線の太さ」と「前景/背景の反転」のみで表現します。

### 設計原則

1. **角丸 0 / 影なし** — `border-radius: 0px`・`box-shadow: none` を全要素に強制。
2. **枠線で構造を示す** — `thin (1px)` / `thick (3px)` / `heavy (5px)` の3段階の枠線のみを使用。
3. **反転で状態を示す** — ホバー（hover）やアクティブ（active）状態は、背景色と前景色を入れ替える（`bg-primary ⇄ bg-background`）。
4. **等幅＋大文字＋角括弧** — 見出しやラベルは等幅フォントかつ大文字（`font-mono uppercase`）、ダイアログ等の見出しは `[タイトル]` 形式を基本とする。
5. **単色主義（Monochrome-first）** — 黒と白を基調とし、アクセントカラーは最小限（情報・警告などのシステム状態色を除く）に留める。

---

## Colors

カラーは CSS 変数（またはテーマ設定）で定義し、ライトモード / ダークモードの両方に対応させます。

### 基本カラー

| トークン名           | Light     | Dark      | 用途                                   |
| -------------------- | --------- | --------- | -------------------------------------- |
| `background`         | `#FFFFFF` | `#000000` | ページ・カード・コンテナの背景         |
| `foreground`         | `#000000` | `#FFFFFF` | 本文テキスト・標準枠線の基準色         |
| `primary`            | `#000000` | `#FFFFFF` | 主要アクション（ボタンの基本塗りなど） |
| `primary-foreground` | `#FFFFFF` | `#000000` | `primary` 背景上のテキスト色           |
| `muted`              | `#F0F0F0` | `#111111` | 補助背景・注記ボックス・非強調領域     |
| `border`             | `#000000` | `#FFFFFF` | 全枠線の基準色                         |
| `input`              | `#F0F0F0` | `#111111` | フォーム入力欄の標準背景               |
| `destructive`        | `#FF0000` | `#FF0000` | 破壊的操作・危険状態・エラー通知       |

### システム状態色（System Colors）

純度の高い原色を使用し、ユーザーが状態を一目で識別できるようにします。

| トークン名      | カラーコード | 用途                                     |
| --------------- | ------------ | ---------------------------------------- |
| `success`       | `#008000`    | 処理完了・正常状態・成功トースト         |
| `warning`       | `#FFA500`    | 注意・警告・制限状態                     |
| `error`         | `#FF0000`    | エラー（`destructive` と同値）           |
| `info` / `blue` | `#0000FF`    | 情報提示・ハイパーリンク・補助アクション |

_※ 必要に応じて `input-hover` / `input-disabled` / `border-disabled` 等の補助トークンを定義し、ホバーや非活性状態の表現に使用します。_

---

## Typography

視認性と作業効率を高めるため、役割に応じたフォントファミリーを定義します。

- **Headline Font**: 重厚感のあるサンセリフ体（見出し・ボタン用）
- **Body Font**: 可読性の高い標準的なサンセリフ体（本文・説明文用）
- **Mono Font**: 等幅フォント（ラベル・データ表示・システムメッセージ用）

### タイプスケール規約

| 種別 / タグ  | フォント種別 | 推奨サイズ / 行間 | 字体スタイル      |
| ------------ | ------------ | ----------------- | ----------------- |
| `h1`         | Headline     | 64px / 1.0        | Standard / Normal |
| `h2`         | Headline     | 48px / 1.05       | Standard / Normal |
| `h3`         | Headline     | 32px / 1.1        | Standard / Normal |
| `h4`         | Body         | 22px / 1.2        | Semi-Bold         |
| `Body Text`  | Body         | 16px / 1.6        | Normal            |
| `Small Text` | Body         | 14px / 1.5        | Normal            |
| `Tiny Text`  | Body         | 12px / 1.4        | Normal            |
| `Mono Text`  | Mono         | 15px / 1.5        | Normal            |

_フォームラベルは `text-xs font-bold uppercase`、補助注記は `text-[10px]` または `text-xs` を推奨します。_

---

## Spacing & Layout

- 密度の高いダッシュボードやデータ入力画面では、**4px / 8px の倍数グリッド** を基本単位とします。
- レイアウト間の推奨ギャップ: `8px` / `16px` / `24px` / `40px`

---

## Border Radius & Elevation

- **Radius（角丸）**: 全て `0px`（例外なし）。
- **Elevation（影・奥行き）**: 影（`box-shadow`）は使用しない。モーダルダイアログ等のオーバーレイ表示は、背景にブラー（`backdrop-blur`）と高透過率の暗色・明色レイヤーを敷くことで前後関係を表現する。

### 枠線は「インタラクティブ（操作可能）」の合図

RawBlock では、**枠線＝押せる／入力できる** というメンタルモデルを徹底します。

- **枠線を付ける要素**: ボタン、入力フォーム（テキスト・セレクト・チェックボックス）、クリック／タップ可能なカードやリスト行。
- **枠線を付けない要素**: 表示専用カード、静的な情報ボックス、非クリックのデータ行。
- **方向指定の区切り線は別扱い**: 上下左右の単一辺に引く区切り線（`border-b-*` 等）は、構造整理のための線であり、囲み枠とは区別して使用する。
- **例外**: モーダル本体の領域輪郭、ドラッグ＆ドロップ等の空状態を示す破線枠、プレビュー画面の画像フレーム。

### 枠線ユーティリティクラス（Border Width Tokens）

任意数値（例: `2px` や `4px` の直書き）は避け、必ず定義された 3 段階のトークンを使用します。

| トークン名     | 太さ | 用途                                                   |
| -------------- | ---- | ------------------------------------------------------ |
| `border-thin`  | 1px  | ディバイダー（区切り線）・控えめな要素境界             |
| `border-thick` | 3px  | 標準枠（標準のカード・入力フォーム・ボタン・モーダル） |
| `border-heavy` | 5px  | フォーカス状態・アクティブ状態・強調表示               |

- 枠線トークンは**幅のみ**を制御し、カラーはベースの境界色（`border`）または状態色クラスを組み合わせます。
- 上下左右の方向指定（`border-t-thick` / `border-b-thin` 等）も同様の太さ規約を適用します。

---

## Component Guidelines

### 1. Button

- 全て `border-thick`・太字大文字（`font-headline uppercase`）・文字間隔広め（`tracking-[2px]`）で統一。
- ホバー時に背景色と前景色を反転し、アクティブ時に `border-heavy` へ変化。
- **Variants**:
- `default`: 主要アクション。Primary 塗り → ホバーで反転。
- `destructive`: 危険・削除操作。赤塗り → ホバーで反転。
- `outline / secondary`: 背景透過または標準背景 → ホバーで Primary 反転。
- `ghost / link`: 枠線なし。`info` カラーの下線テキスト。

### 2. Input / Form Controls

- 背景色は `input` トークン、枠線は `border-thick`、文字は `font-mono`。
- フォーカス時はアウトラインリングを消去し、枠線を `border-heavy` に強調する。
- バリデーションエラー時は枠線を `error`（`destructive`）色へ変更。

### 3. Card

- **Variants**:
- `default`: 枠線なし。表示専用コンテンツ用。
- `interactive`: 枠線あり（`border-thick`）+ ホバーによる反転。クリック可能な要素用。
- `bordered`: 枠線あり。ダイアログや明確に区切りたい領域用。
- `elevated`: 太枠（`border-heavy`）による強強調用。

### 4. Modal / Dialog

- 背景オーバレイ（クリックでキャンセル可能）と中央配置の `bordered` カードで構成。
- ヘッダーは `[タイトル]` の形式を用い、右上に閉じるボタン（`×`）を配置。
- キーボード操作（`Escape` キーによるクローズ）およびアクセシビリティ標準（`role="dialog"`, `aria-modal="true"`）に準拠。

### 5. Form Field Architecture

- **FormField Wrapper**: ラベル（`text-xs font-bold uppercase`）と入力要素をアクセシブルに紐付け。必須項目には `*` を付与。
- **Confirmation Dialog**: 未保存データが存在する状態での画面離脱時や、危険な破壊的操作を行う前に必ず明示的な確認ダイアログを表示する。

---

## Form & Data Handling Rules

1. **自動保存と明示的保存の明確化**

- 編集画面において「フォーカスが外れたタイミング（`onBlur`）での自動保存」を採用する場合は、画面上に「フォーカスを外すと自動保存されます」などの明示的なバナー（`EditModeBanner`）を設置する。

2. **ダイアログによる制御**

- ブラウザ標準の `alert()` や `confirm()` はデザインが崩れ、操作を阻害する可能性があるため使用を禁止する。必ずデザインシステムに準拠したアプリ内ダイアログコンポーネントを使用する。

---

## Do's and Don'ts

1. **Do** 太枠とコントラスト反転で状態を表現する。
   **Don't** グラデーション・ドロップシャドウ・角丸（border-radius）を追加しない。
2. **Do** ラベルや見出しは `font-mono uppercase` を使用し、ダイアログの見出しは `[タイトル]` 形式で統一する。
   **Don't** タイトル表現やフォント種別を画面ごとにバラバラにしない。
3. **Do** 状態表現には定義されたカラー（`success` / `warning` / `error` / `info`）を使用する。
   **Don't** カラーコードやフレームワーク標準の色（例: `text-green-600` など）をスタイルに直接ハードコードしない。
4. **Do** 主なアクセントカラーは1色に絞り、単色主義を維持する。
5. **Do** 共通 UI コンポーネント（Button, Input, Card, Modal 等）を再利用する。
   **Don't** 同じスタイル文字列や JSX 構造を無闇にコピー＆ペーストして独自の素の UI を作成しない。
6. **Do** トークンやスタイルの変更は CSS 変数 / デザインシステム定義を起点とし、仕様書を更新する。
7. **Do** キーボード操作（`Escape` キー操作、Tab フォーカスの可視化）やスクリーンリーダー対応（ラベル紐付け）を徹底する。
