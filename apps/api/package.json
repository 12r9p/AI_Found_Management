{
  "name": "@found/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "NODE_ENV=development bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "cf-dev": "wrangler dev --var NODE_ENV:development --config wrangler.toml --config ../images/wrangler.toml",
    "seed": "bun run src/scripts/seed.ts",
    "migrate": "bun run src/scripts/migrate.ts",
    "migrate:local": "wrangler d1 migrations apply found-db --local",
    "migrate:remote": "wrangler d1 migrations apply found-db --remote",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@elysia/openapi": "1.4.15",
    "@elysiajs/cors": "1.4.2",
    "elysia": "1.4.29"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "@types/bun": "^1.1.14",
    "typescript": "6.0.3",
    "wrangler": "4.118.0"
  }
}
